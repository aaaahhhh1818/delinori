<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noriteo.delinori.saleboard.mapper.ReplyMapper">

    <insert id="insert">
        insert into reply_saleboard (sno, replyer, reply)
        values (#{sno}, #{replyer}, #{reply})
    </insert>

    <select id="getListWithBoard" resultType="com.noriteo.delinori.saleboard.domain.Reply">
        select * from reply_saleboard where sno = #{sno} order by rno asc
    </select>

    <delete id="delete">
        delete from reply_saleboard where rno = #{rno}
    </delete>

    <update id="update">
        update reply_saleboard set reply = #{reply}, modDate = now() where rno = #{rno}
    </update>

</mapper>